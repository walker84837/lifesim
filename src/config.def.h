#ifndef CONFIG_DEF_H_
#define CONFIG_DEF_H_

// Number of cells in one dimension of the grid
static int GRID_SIZE = 50;

// Default speed in CLI arguments
static float DEFAULT_SPEED = 1.0f;

// Screen width and height of the window
static int SCREEN_WIDTH = 800;
static int SCREEN_HEIGHT = 800;

// Speed increment and decrement step
static float SPEED_STEP = 0.1f;

#endif  // CONFIG_DEF_H_
